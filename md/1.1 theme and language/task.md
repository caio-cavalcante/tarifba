# Tasks

- [x] Update `style.css` with CSS variables for light theme and `data-theme` selectors.
- [x] Update `index.html` with `data-i18n` attributes and add the FAB for toggles.
- [x] Update `script.js` with translation dictionary, toggle logic, and replace hardcoded JS strings.
- [x] Suggest test suite approaches for the project.
