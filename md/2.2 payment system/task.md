# Refactor Payment System to Running Ledger

- [x] Update `translations` in `script.js` with new keys and modified WhatsApp message.
- [x] Update `fetchData` in `script.js` to load the `payments` table and compute `totalPaid` dynamically.
- [x] Update `updateDashboard` in `script.js` to replace the "Mark as Paid" button with "Register Payment" button and updated WhatsApp link generation.
- [x] Implement `openPaymentModal` in `script.js`.
- [x] Implement `form-register-payment` submission handler in `script.js`.
- [x] Add the Register Payment modal markup to `index.html`.
- [x] Remove `window.markPaid` from `script.js`.
